---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infrawrench_accounts Data Source - infrawrench"
subcategory: ""
description: |-
  Cloud accounts connected to the organization.
  The reason to reach for this is to turn an account's display name — the thing a human knows — into the account id that infrawrench_allocation_rule's match.account_id and infrawrench_billing_rule's match.account_id want, without pasting an opaque id into the configuration.
  The listing carries no credential material. An account's credentials sit behind a separate secrets:read route that this data source deliberately never calls, so no secret can reach the state file through it.
---

# infrawrench_accounts (Data Source)

Cloud accounts connected to the organization.

The reason to reach for this is to turn an account's display name — the thing a human knows — into the account id that `infrawrench_allocation_rule`'s `match.account_id` and `infrawrench_billing_rule`'s `match.account_id` want, without pasting an opaque id into the configuration.

The listing carries no credential material. An account's credentials sit behind a separate `secrets:read` route that this data source deliberately never calls, so no secret can reach the state file through it.



<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `plugin_id` (String) Restricts the result to accounts belonging to one plugin, e.g. `aws`. The API has no server-side filter on this route, so the provider fetches the whole listing and filters client-side — it saves configuration noise, not a round trip.

### Read-Only

- `accounts` (Attributes List) Matching accounts, in the order the API returned them. (see [below for nested schema](#nestedatt--accounts))
- `id` (String) The organization id. A plural data source has no identity of its own; this is the conventional placeholder.

<a id="nestedatt--accounts"></a>
### Nested Schema for `accounts`

Read-Only:

- `bastion_id` (String) Id of the bastion the account is reached through, null when it is reached directly.
- `created_at` (String) RFC 3339 timestamp of when the account was connected.
- `display_name` (String) Name the account was given when it was connected.
- `id` (String) Account id, as used by rule `match.account_id`.
- `plugin_id` (String) Id of the plugin the account belongs to.
