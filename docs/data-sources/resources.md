---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infrawrench_resources Data Source - infrawrench"
subcategory: ""
description: |-
  The cloud resources synced from one account.
  This provider never writes cloud resources — ejecting them to HCL is what Terraform export is for, and creating them is your cloud provider's job. Reading them matters because infrawrench_probe, infrawrench_schedule and infrawrench_log_query all address a resource by its Infrawrench id, and hard-coding one of those in HCL is exactly what this data source exists to avoid.
  The listing is what the last sync found. A resource created five minutes ago may not be here yet, which makes this a poor thing to depend on inside the same apply that created it.
  Each resource's fields and outputs are deliberately not exposed: they are unbounded provider-shaped blobs, and putting them in state would write a resource's entire configuration — including anything sensitive in it — into every state file that reads this.
---

# infrawrench_resources (Data Source)

The cloud resources synced from one account.

This provider never *writes* cloud resources — ejecting them to HCL is what Terraform export is for, and creating them is your cloud provider's job. Reading them matters because `infrawrench_probe`, `infrawrench_schedule` and `infrawrench_log_query` all address a resource by its Infrawrench id, and hard-coding one of those in HCL is exactly what this data source exists to avoid.

The listing is what the last sync found. A resource created five minutes ago may not be here yet, which makes this a poor thing to depend on inside the same apply that created it.

Each resource's fields and outputs are deliberately **not** exposed: they are unbounded provider-shaped blobs, and putting them in state would write a resource's entire configuration — including anything sensitive in it — into every state file that reads this.

## Example Usage

```terraform
data "infrawrench_accounts" "aws" {
  plugin_id = "aws"
}

# The listing is what the last sync found, so a resource created five minutes
# ago may not be here yet — a poor thing to depend on inside the same apply
# that created it.
data "infrawrench_resources" "api" {
  account_id       = data.infrawrench_accounts.aws.accounts[0].id
  resource_type_id = "ec2_instance"
  name_contains    = "api"
}

output "api_resource_id" {
  value = data.infrawrench_resources.api.resources[0].id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `account_id` (String) Account to list. Required rather than optional because the API has no org-wide resource listing — resources are read per account.

### Optional

- `name_contains` (String) Keep only resources whose display name contains this substring, matched case-insensitively. Filtered client-side.

A filter that matches several resources is not an error here — the data source returns them all, and it is the configuration's job to pick one. Narrow it until `length(...) == 1` if you are going to index into it.
- `resource_type_id` (String) Keep only resources of this type, e.g. `ec2_instance`. Filtered client-side after the listing.

### Read-Only

- `id` (String) The account id the listing was read from. A plural data source has no identity of its own.
- `resources` (Attributes List) Matching resources, in the order the API returned them. (see [below for nested schema](#nestedatt--resources))

<a id="nestedatt--resources"></a>
### Nested Schema for `resources`

Read-Only:

- `account_id` (String) Account the resource belongs to.
- `display_name` (String) Name as the provider reports it.
- `external_id` (String) The provider's own identifier — an instance id, an ARN — or null when the plugin does not carry one.
- `id` (String) Infrawrench resource id — the value `resource_id` takes on a probe, a schedule or a log stream.
- `plugin_id` (String) Plugin the resource belongs to.
- `resource_type_id` (String) Resource type within the plugin.
