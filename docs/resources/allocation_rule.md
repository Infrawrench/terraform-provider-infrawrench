---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infrawrench_allocation_rule Resource - infrawrench"
subcategory: ""
description: |-
  A rule that routes matching spend to an infrawrench_cost_centre.
  Allocation is computed at query time from the whole rule set, so adding, reordering or deleting a rule restates every historical report immediately — nothing is written back onto the stored cost rows.
---

# infrawrench_allocation_rule (Resource)

A rule that routes matching spend to an `infrawrench_cost_centre`.

Allocation is computed at query time from the whole rule set, so adding, reordering or deleting a rule restates every historical report immediately — nothing is written back onto the stored cost rows.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cost_centre_id` (String) Cost centre this rule allocates matched spend to.
- `priority` (Number) Evaluation order, 0–100000. The lower priority evaluates first and the first match wins, so a row allocated by a rule is never reconsidered by a later one. Priorities are not unique-constrained: two rules may share one, and which of them wins is then unspecified, so give rules you care about distinct priorities.

### Optional

- `match` (Block, Optional) Conditions a cost row must meet. Whichever fields are set are ANDed together, and an empty block matches everything — which is a legitimate catch-all when paired with the lowest priority in the set. (see [below for nested schema](#nestedblock--match))

### Read-Only

- `id` (String) Server-assigned rule id. Use it with `terraform import`.

<a id="nestedblock--match"></a>
### Nested Schema for `match`

Optional:

- `account_id` (String) Match spend from one connected account. See the `infrawrench_accounts` data source.
- `plugin_id` (String) Match spend from one provider plugin. See the `infrawrench_plugins` data source.
- `service` (String) Match one provider service name, as it appears in the cost data.
- `tag_key` (String) Match rows carrying this tag key. On its own it matches any value; pair it with `tag_value` to pin the value.
- `tag_value` (String) Value the `tag_key` tag must hold. Requires `tag_key`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The id is in the URL when you open the object in the app, and in the
# --json output of the Infrawrench CLI.
terraform import infrawrench_allocation_rule.example 4e5f6a7b-8c9d-0e1f-2a3b-4c5d6e7f8a9b
```
