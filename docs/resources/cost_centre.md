---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infrawrench_cost_centre Resource - infrawrench"
subcategory: ""
description: |-
  A node in the cost centre tree that infrawrench_allocation_rule allocates spend to.
  Deleting a cost centre is not blocked by having children or rules. The server re-parents the children onto the deleted centre's parent and deletes the centre's allocation rules; historical spend is left untouched, since allocation is computed at query time rather than stored on the cost rows.
---

# infrawrench_cost_centre (Resource)

A node in the cost centre tree that `infrawrench_allocation_rule` allocates spend to.

Deleting a cost centre is not blocked by having children or rules. The server re-parents the children onto the deleted centre's parent and deletes the centre's allocation rules; historical spend is left untouched, since allocation is computed at query time rather than stored on the cost rows.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name, 1–120 characters.

### Optional

- `description` (String) Free-text description, up to 2000 characters.
- `parent_id` (String) Id of the parent cost centre. Omit it to keep the centre at the root. Maximum nesting depth is 4, and a parent that would close a cycle is rejected.

### Read-Only

- `id` (String) Server-assigned cost centre id. Use it with `terraform import`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The id is in the URL when you open the object in the app, and in the
# --json output of the Infrawrench CLI.
terraform import infrawrench_cost_centre.example 9a8b7c6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
```
