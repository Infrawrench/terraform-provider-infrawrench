---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infrawrench_exchange_rate Resource - infrawrench"
subcategory: ""
description: |-
  One stated exchange rate, effective from a day.
  A given day converts at the rate with the greatest effective_from on or before it, so historical periods keep the rate that applied then and restating a rate today cannot restate last quarter's totals. A day earlier than every stated rate has no rate at all.
  This is exactly the kind of number that belongs in review: it restates every converted total the organization reports, in the digest that goes to the whole team and in the budget alerts that page people.
---

# infrawrench_exchange_rate (Resource)

One stated exchange rate, effective from a day.

A given day converts at the rate with the greatest `effective_from` on or before it, so historical periods keep the rate that applied then and restating a rate today cannot restate last quarter's totals. A day earlier than every stated rate has no rate at all.

This is exactly the kind of number that belongs in review: it restates every converted total the organization reports, in the digest that goes to the whole team and in the budget alerts that page people.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `effective_from` (String) Inclusive day this rate starts applying, as `YYYY-MM-DD`. Together with the two currencies it is the rate's natural key: restating a rate for a day it already covers replaces it rather than adding a second one the reader has to choose between.
- `from_currency` (String) ISO 4217 code, upper-case — the currency being converted **from**.
- `rate` (String) Multiply an amount in `from_currency` by this to get `to_currency`, e.g. `"1.0850000000"`.

A decimal **string**, not a number: it is stored in a `numeric(20, 10)` column so the digits your finance system used survive the round trip exactly, and a float could not promise that.
- `to_currency` (String) ISO 4217 code, upper-case — the currency being converted **to**.

### Read-Only

- `id` (String) Server-assigned rate id. Use it with `terraform import`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The id is in the URL when you open the object in the app, and in the
# --json output of the Infrawrench CLI.
terraform import infrawrench_exchange_rate.example 2c3d4e5f-6a7b-8c9d-0e1f-2a3b4c5d6e7f
```
