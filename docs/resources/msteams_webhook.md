---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infrawrench_msteams_webhook Resource - infrawrench"
subcategory: ""
description: |-
  A Microsoft Teams webhook registered as an alert destination.
  Registering it makes it available; what arrives there is decided by infrawrench_alert_routing.
---

# infrawrench_msteams_webhook (Resource)

A Microsoft Teams webhook registered as an alert destination.

Registering it makes it available; what arrives there is decided by `infrawrench_alert_routing`.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `label` (String) Display name for the destination, e.g. `#alerts`. The only mutable attribute.
- `url` (String, Sensitive) The webhook URL from a Teams **Workflows** automation.

Must be https and on a Microsoft-operated host — `*.api.powerautomate.com`, `*.api.powerplatform.com`, `*.logic.azure.com`, `*.flow.microsoft.com`, or a legacy `*.webhook.office.com` connector. Anything else is refused, since a webhook URL is a bearer credential and posting alerts to an arbitrary host is not a thing to allow by accident.

Write-only: no route returns it, so the provider cannot detect drift on it and changing it replaces the registration.

### Read-Only

- `id` (String) Infrawrench's id for this webhook. `terraform import` works, but the stored URL is not recoverable — supply it in configuration afterwards.
- `url_hint` (String) Non-secret hint at the stored URL — its host and last four characters. The only readable signal that the stored credential is the one you think it is.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The id is in the URL when you open the object in the app, and in the
# --json output of the Infrawrench CLI.
terraform import infrawrench_msteams_webhook.example 0a1b2c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
```
